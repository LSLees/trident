{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Verilog (build+run)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/sim.vvp",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "externalConsole": true,
      "preLaunchTask": "Verilog: build + run"
    }
  ],
  "compounds": [
    {
      "name": "Verilog: F5",
      "configurations": ["Run Verilog (build+run)"]
    }
  ]
}
